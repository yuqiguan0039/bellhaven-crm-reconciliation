# Copy this file to .env and replace the placeholder with your personal token.
# Never commit the completed .env file.
export CRM_API_TOKEN='replace_with_your_personal_api_token'

# CRM writes are disabled unless this line is uncommented.
# Enable it only when you are ready to apply approved changes.
# export ALLOW_CRM_WRITES='1'
